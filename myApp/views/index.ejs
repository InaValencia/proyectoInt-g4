<!DOCTYPE html>
<html lang="es">
	
<!-- Include Head -->

<%- include('./partials/head')  %> 

<body>
<h1 style="color:red"> <%= product.model %> </h1>
<h2 style="color:red"> <%= product.description %> </h2>

	<!-- Include Header -->
	<%if (product.logueado){%>
		<%- include("partials/headerLogueado.ejs") %>
		<%} else { %>
		<%- include("partials/headerNoLogueado.ejs") %>
	<% } %>
	<!-- Banner -->
	<div class="banner">
		<img src="images/bannerJustHype.png" alt="Banner de ofertas">
	</div>
	<!-- /Banner -->


	<!-- acá voy a hacer un bucle que recorra el array de productos y muestre un div por cada producto -->
	<!-- Novedades -->
	<div class="container products-wrapper">
		<div class="row">
			<div class="col-12">
				<h2 class="products-title">New Releases</h2>
			</div>
			<!-- Show the "ultimos" products -->
			<% for (let i = 0; i < product.products.length; i++) { %>
			<div class="col-12 col-sm-6 col-lg-3">
				<section class="product-box">
					<a href="/products">
						<figure class="product-box_image">
							<img src="<%= product.products[i].image %>"  alt="shoes">
						</figure>
						<article class="product-box_data">
							<h2> <%= product.products[i].model  %>  </h2>
							<p> <%= product.products[i].description  %> </p>
							<p>Comments: 5</p>
						</article>
					</a>
				</section>
			</div>
			<% } %>
		</div>
	</div>
	<!-- /Novedades -->
	<!-- Más comentados -->
	<div class="container products-wrapper">
		<div class="row">
			<div class="col-12">
				<h2 class="products-title">Most popular</h2>
			</div>
			
			<% for (let i = 0; i < product.mostPopular.length; i++) { %>
				<div class="col-12 col-sm-6 col-lg-3">
					<section class="product-box">
						<a href="/products">
							<figure class="product-box_image">
								<img src="<%= product.mostPopular[i].image %>"  alt="shoes">
							</figure>
							<article class="product-box_data">
								<h2> <%= product.mostPopular[i].model  %>  </h2>
								<p> <%= product.mostPopular[i].description  %> </p>
								<p>Comentarios: 12</p>
							</article>
						</a>
					</section>
				</div>
				<% } %>	
		</div>
	</div>
	<!-- /Más comentados -->
	<!-- footer -->
	<%- include('./partials/footer.ejs')  %> 
</body>	
</html>